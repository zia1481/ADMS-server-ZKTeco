<div class="empty-state">
    <i class="bi {{ $icon ?? 'bi-inbox' }} empty-icon"></i>
    <div class="empty-title">{{ $title ?? 'No records found' }}</div>
    <p class="empty-desc">{{ $description ?? 'Try changing your filters or search criteria.' }}</p>
</div>
